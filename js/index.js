'use strict';

/* const Player = function (name) {
  this.name = name;
  this.lvl = 1;
  this.points = 0;
};

Player.prototype.gainXp = function (num) {
  this.points += num;
  console.log(this.points);
  if (this.points >= 10) {
    this.lvl += 1;
    this.points -= 10;
  }
};

Player.prototype.describe = function () {
  console.log(
    `Player: ${this.name} | Level: ${this.lvl} | Points:${this.points}`
  );
};

const boby = new Player('Bobby');
const marty = new Player('marty');

boby.gainXp(5);
boby.gainXp(6);
boby.gainXp(6);

boby.gainXp(6);
marty.gainXp(6);
marty.gainXp(6);
marty.gainXp(6);

boby.describe();
marty.describe();
 */
